{% if vpp_ver < 1904 %}
tap connect uplink
tap connect vxlan
{% else %}
create int rdma host-if enp94s0f1 name rdma-0
set interface state rdma-0 up
set interface ip address rdma-0 1.1.1.2/24
create host-interface name vpp0
set interface state host-vpp0 up
{% endif %}
